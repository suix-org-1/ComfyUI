name: Sync Fork with Upstream Hourly

on:
  schedule:
    - cron: '0 * * * *'  # 每小时 UTC 整点执行
  workflow_dispatch:     # 允许手动触发

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout your fork
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Add upstream remote
        run: |
          git remote add upstream https://github.com/comfyanonymous/ComfyUI.git
          git fetch upstream

      - name: Sync main branch
        run: |
          git checkout main
          git merge upstream/main --ff-only
          git push origin main

      # 👇 如果你还想同步其他分支（比如你有 custom 分支），可以取消注释下面部分
      # - name: Sync custom branch (optional)
      #   run: |
      #     git checkout custom || git checkout -b custom
      #     git merge upstream/main --ff-only
      #     git push origin custom
